CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

project (gambigames)

ADD_EXECUTABLE(gambi main.c)
 
INCLUDE(FindPkgConfig)
PKG_SEARCH_MODULE(SDL2 REQUIRED sdl2)
INCLUDE_DIRECTORIES(${SDL2_INCLUDE_DIRS})
TARGET_LINK_LIBRARIES(gambi ${SDL2_LIBRARIES})
